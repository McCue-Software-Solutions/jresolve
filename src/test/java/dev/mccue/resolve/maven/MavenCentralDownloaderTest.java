package dev.mccue.resolve.maven;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import java.io.IOException;
import java.net.HttpURLConnection;
import java.net.URL;

import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;

public final class MavenCentralDownloaderTest {

    @BeforeEach
    public void setup() throws IOException {
//        URL u = mock(URL.class);
//        HttpURLConnection h = mock(HttpURLConnection.class);
//        when(u.openConnection()).thenReturn(h);
//        when(h.getResponseCode()).thenReturn(200);
    }


    @Test
    public void testConstructor() throws Exception {
//        new MavenCentralDownloader("junit", "junit", "1.0", "/downloads");
    }

}
